# MVP День 1 — 16 декабря 2025

## Решения из обсуждения (15 дек)

| Вопрос | Решение |
|--------|---------|
| Android туннель | Kotlin Native (одно приложение: мессенджер или сервер по логину) |
| VK | Оба: Community API + User API |
| Реверс приоритет | Авито + Max параллельно |
| Сервер туннеля | Новый выделенный сервер |
| БД | PostgreSQL (потом экстрактор → Neo4j) |
| Web UI | Отдельный разработчик, Swagger API |
| Telegram User | Pyrogram |
| Авторизация туннеля | mTLS (сертификаты) |
| Оффлайн | Да, кэш + отложенная отправка |
| Демо | Все каналы работают |

## Цель дня

Подготовить инфраструктуру и начать реверс-инжиниринг.

---

## Архитектура каналов

### На сервере (прямое подключение)

| Канал | Компонент | Статус |
|-------|-----------|--------|
| Telegram Bot | mcp-telegram | ✅ готов |

### Через туннель (Android)

| Канал | Компонент | Статус |
|-------|-----------|--------|
| Telegram User | telethon-service | ❌ нужно писать |
| WhatsApp | mcp-whatsapp-baileys | ✅ готов (Node.js) |
| Max User | max-reverse | ❌ нужно реверсить |
| Авито User | avito-reverse | ❌ нужно реверсить |
| VK User | vk-user | ❌ нужно писать |

---

## Архитектура туннеля

```
┌─────────────────────────────────────────────────────────────┐
│                         СЕРВЕР                              │
│                                                             │
│  ┌─────────────────┐     ┌─────────────────────────────┐   │
│  │ mcp-telegram    │────►│           n8n               │   │
│  │ (Bot API)       │     │    (Message Hub Backend)    │   │
│  └─────────────────┘     │                             │   │
│                          │    - Workflows для каналов  │   │
│  ┌─────────────────┐     │    - Хранение сообщений    │   │
│  │ Tunnel Server   │────►│    - WebSocket для UI       │   │
│  │ (FastAPI + WS)  │     │                             │   │
│  └────────┬────────┘     └─────────────────────────────┘   │
│           │                                                │
└───────────┼────────────────────────────────────────────────┘
            │ WebSocket (WSS)
            │
┌───────────┼────────────────────────────────────────────────┐
│           │          📱 ANDROID (Tunnel Client)            │
│  ┌────────┴────────┐                                       │
│  │  Tunnel Service │  ← Foreground Service                 │
│  │  (Kotlin)       │  ← WebSocket to Server                │
│  └────────┬────────┘                                       │
│           │ HTTP localhost                                 │
│  ┌────────┴────────────────────────────────────────────┐  │
│  │  Локальные сервисы (Termux / Native):               │  │
│  │                                                      │  │
│  │  ┌─────────────────┐  ┌─────────────────┐          │  │
│  │  │ Baileys (WA)    │  │ Telethon (TG)   │          │  │
│  │  │ Node.js :3001   │  │ Python :3002    │          │  │
│  │  └─────────────────┘  └─────────────────┘          │  │
│  │                                                      │  │
│  │  ┌─────────────────┐  ┌─────────────────┐          │  │
│  │  │ Avito Reverse   │  │ Max Reverse     │          │  │
│  │  │ Python :3003    │  │ Python :3004    │          │  │
│  │  └─────────────────┘  └─────────────────┘          │  │
│  │                                                      │  │
│  │  ┌─────────────────┐                               │  │
│  │  │ VK User         │                               │  │
│  │  │ Python :3005    │                               │  │
│  │  └─────────────────┘                               │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## Задачи на день 1

### 1. Tunnel Server (сервер)

Файл: `MCP/tunnel-server/`

```python
# FastAPI + WebSocket
# - Регистрация tunnel clients
# - Проксирование запросов в туннель
# - Получение сообщений из туннеля → n8n
```

**Endpoints:**
- `WS /tunnel/{client_id}` — WebSocket соединение с туннелем
- `POST /tunnel/{client_id}/send` — отправить сообщение через туннель
- `GET /tunnel/{client_id}/status` — статус туннеля

### 2. Tunnel Protocol

```json
// Server → Client (request)
{
  "id": "uuid",
  "type": "request",
  "service": "whatsapp",
  "action": "send_message",
  "payload": {
    "to": "79001234567",
    "text": "Hello"
  }
}

// Client → Server (response)
{
  "id": "uuid",
  "type": "response",
  "success": true,
  "data": {...}
}

// Client → Server (event)
{
  "type": "event",
  "service": "whatsapp",
  "event": "message",
  "data": {
    "from": "79001234567",
    "text": "Hello!"
  }
}
```

### 3. Определить приоритеты сервисов

| Сервис | Приоритет | Причина |
|--------|-----------|---------|
| WhatsApp Baileys | P0 | Уже готов, нужен для демо |
| Telegram User | P1 | Нужен Telethon |
| Авито Reverse | P2 | Нужен реверс |
| Max Reverse | P2 | Нужен реверс |
| VK User | P3 | Позже |

---

## Вопросы для обсуждения

1. **Android туннель**: Kotlin native или Termux?
   - Kotlin: сложнее, но нативно
   - Termux: проще, Python/Node работают

2. **VK**: Bot API достаточно или нужен User?

3. **Приоритет реверса**: Авито или Max первым?

---

## Ожидаемый результат дня 1

- [ ] Tunnel Server skeleton (FastAPI + WebSocket)
- [ ] Tunnel Protocol спецификация
- [ ] Android App skeleton (WebSocket client)
- [ ] Тест: Server ↔ Android WebSocket connection
