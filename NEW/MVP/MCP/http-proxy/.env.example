HOST=0.0.0.0
PORT=3010

# Limits
MAX_TIMEOUT=60
MAX_RESPONSE_SIZE=10485760

# Enable headless browser for /render endpoint
# Requires: pip install playwright && playwright install chromium
BROWSER_ENABLED=false
