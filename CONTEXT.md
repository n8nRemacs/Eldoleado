# Eldoleado — База Знаний (Knowledge Base)

> **Для Claude:** Это система ведения базы знаний проекта. Ты отвечаешь за её актуальность, полноту и структуру. Используй эти инструменты для понимания контекста и управления знаниями.

**Последнее обновление:** 2025-12-06
**Версия KB:** 1.0

---

## 🗄️ Структура базы знаний

### Где хранятся данные

```
PostgreSQL (185.221.214.83:6544)
│
├── project_components      ← Все компоненты системы (workflows, tables, API, MCP...)
├── component_relations     ← Связи между компонентами
├── component_fields        ← Поля/свойства компонентов
├── workflow_nodes          ← Детали нод внутри workflows
├── node_connections        ← Связи между нодами
├── doc_chunks              ← Фрагменты документов с embeddings (RAG)
├── doc_links               ← Связи документов с компонентами
└── search_history          ← История поисков (для аналитики)

Файловая система
│
├── CONTEXT.md              ← ЭТОТ ФАЙЛ (состояние, решения, планы)
├── Plans/*.md              ← Документы планирования (индексируются в RAG)
├── docs/flows/*.md         ← Автогенерируемая документация workflows
└── CLAUDE.md               ← Быстрые команды и инструкции
```

---

## 🔧 Операции с базой знаний

### 1. ЧТЕНИЕ — Получение контекста

**Найти компонент по имени:**
```sql
SELECT * FROM project_components
WHERE name ILIKE '%telegram%' AND status = 'active';
```

**Найти связи компонента:**
```sql
SELECT
    c2.type, c2.name, r.relation_type
FROM component_relations r
JOIN project_components c2 ON c2.id = r.to_component_id
WHERE r.from_component_id = (
    SELECT id FROM project_components WHERE name = 'BAT IN Telegram'
);
```

**Найти все workflows, работающие с таблицей:**
```sql
SELECT DISTINCT workflow_name
FROM workflow_nodes
WHERE details->>'sql' ILIKE '%appeals%';
```

**Семантический поиск по документам (когда RAG работает):**
```bash
curl "http://localhost:8090/mcp/ask?query=как работает обработка сообщений"
```

**Получить полный контекст по теме:**
```bash
curl -X POST http://localhost:8090/context \
  -H "Content-Type: application/json" \
  -d '{"topic": "воронка продаж", "max_depth": 2}'
```

---

### 2. ЗАПИСЬ — Добавление знаний

**Добавить новый компонент:**
```sql
SELECT add_component(
    'mcp_server',                    -- тип
    'MCP Docs RAG',                  -- название
    'Infrastructure',                -- категория
    'MCP/mcp-docs-rag/app.py',      -- путь к файлу
    'Семантический поиск по документации'  -- описание
);
```

**Добавить связь между компонентами:**
```sql
SELECT add_relation(
    'mcp_server', 'MCP Docs RAG',    -- откуда (тип, имя)
    'table', 'doc_chunks',           -- куда (тип, имя)
    'reads_from',                    -- тип связи
    'Читает чанки документов для поиска'  -- описание
);
```

**Типы компонентов:**
- `workflow` — n8n workflow
- `table` — таблица PostgreSQL
- `mcp_server` — MCP сервер
- `api_endpoint` — API endpoint
- `android_activity` / `android_viewmodel` / `android_repository` — Android
- `script` — Python/JS скрипт
- `doc` — документ

**Типы связей:**
- `calls` — вызывает
- `reads_from` — читает из
- `writes_to` — пишет в
- `triggers` — триггерит
- `depends_on` — зависит от
- `implements` — реализует
- `uses` — использует

---

### 3. ОБНОВЛЕНИЕ — Актуализация знаний

**Обновить описание компонента:**
```sql
UPDATE project_components
SET description = 'Новое описание',
    updated_at = NOW()
WHERE type = 'workflow' AND name = 'BAT IN Telegram';
```

**Изменить статус компонента:**
```sql
UPDATE project_components
SET status = 'deprecated',  -- active | deprecated | planned | in_progress
    updated_at = NOW()
WHERE name = 'Old Component';
```

**Массовая синхронизация из n8n:**
```bash
python scripts/full_sync.py
```

**Переиндексация документов для RAG:**
```bash
python MCP/mcp-docs-rag/indexer.py --path Plans
python MCP/mcp-docs-rag/indexer.py --link
python MCP/mcp-docs-rag/indexer.py --components
```

---

### 4. УДАЛЕНИЕ — Очистка устаревшего

**Удалить компонент (каскадно удалит связи):**
```sql
DELETE FROM project_components WHERE name = 'Old Component';
```

**Очистить устаревшие связи:**
```sql
DELETE FROM component_relations
WHERE verified_at < NOW() - INTERVAL '30 days';
```

---

## 📊 Текущее состояние KB

### Статистика (обновлять периодически)

```sql
-- Выполни этот запрос для актуальной статистики
SELECT
    'components' as metric, COUNT(*) as count FROM project_components WHERE status = 'active'
UNION ALL
SELECT 'relations', COUNT(*) FROM component_relations
UNION ALL
SELECT 'workflow_nodes', COUNT(*) FROM workflow_nodes
UNION ALL
SELECT 'doc_chunks', COUNT(*) FROM doc_chunks;
```

**Последняя проверка:** 2025-12-06
| Метрика | Количество |
|---------|------------|
| Компоненты | ~150 |
| Связи | ~300 |
| Ноды workflows | ~2000 |
| Чанки документов | 0 (нужна индексация) |

---

## 🎯 Состояние проекта

---

### 🟢 ГОТОВО (Production)

#### MCP Серверы

**RU Server (45.144.177.128)**
| Модуль | Порт | Версия | Описание |
|--------|------|--------|----------|
| mcp-avito | 8765 | v2.0.0 multi-tenant | Avito Messenger API adapter |
| mcp-vk | 8767 | v2.0.0 multi-tenant | VK Community API adapter |
| mcp-max | 8768 | v2.0.0 multi-tenant | MAX (VK Teams) API adapter |
| mcp-form | 8770 | v1.0.0 | Web-формы обратной связи |
| api-android | 8780 | - | API Gateway для Android |

**Finnish Server (217.145.79.27)**
| Модуль | Порт | Версия | Описание |
|--------|------|--------|----------|
| mcp-telegram | 8767 | v2.0.0 multi-tenant | Telegram Bot API proxy |
| mcp-whatsapp | 8766 | v2.0.0 multi-tenant | WhatsApp (Wappi.pro) API adapter |

**Shared Storage**
| Компонент | Описание |
|-----------|----------|
| Redis (avito-redis) | Primary storage для аккаунтов (RU server) |
| Redis (mcp-redis) | Primary storage для аккаунтов (Finnish server) |
| PostgreSQL | Backup storage в таблице `channel_accounts` |

#### n8n Workflows (активные)
| Workflow | Описание |
|----------|----------|
| BAT IN VK | Входящие сообщения VK |
| BAT IN WhatsApp | Входящие сообщения WhatsApp |
| BAT IN Avito | Входящие сообщения Avito |
| BAT IN Form | Входящие с web-форм |
| BAT IN Phone | Входящие звонки |
| BAT AI Universal Worker 1 | AI обработка (extraction) |
| BAT Out Processor 1 | Исходящие сообщения |
| BAT Neo4j Sync | Синхронизация с графом |
| BAT Neo4j Touchpoint Register | Регистрация касаний |
| BAT Fingerprint Tracker | Отслеживание fingerprints |
| BAT Short Link Manager | Короткие ссылки |
| API_Android_* (10 шт) | API для Android приложения |

#### Android App
| Модуль | Описание |
|--------|----------|
| MainActivity | Список обращений |
| AppealDetailActivity | Детали обращения, чат |
| LoginActivity | Авторизация |
| CallRecording | Запись звонков |
| FCM | Push-уведомления |
| API Layer | Retrofit + Repository pattern |

#### База данных (17 миграций)
| Область | Таблицы |
|---------|---------|
| Core | tenants, clients, appeals, messages |
| Devices | appeal_devices, repair_categories, issue_types |
| Funnel | funnel_stages, appeal_stage_history |
| Marketing | promo_campaigns, short_links, fingerprints |
| AI | ai_prompts, ai_extraction_queue |
| Knowledge Base | project_components, component_relations, workflow_nodes |

#### Скрипты автоматизации
| Скрипт | Назначение |
|--------|------------|
| full_sync.py | Полная синхронизация KB |
| trace_flow.py | Трассировка потоков |
| update_flow_docs.py | Генерация документации |
| populate_*.py | Наполнение KB |

---

### 🟡 В РАЗРАБОТКЕ

| Модуль | Статус | Что осталось |
|--------|--------|--------------|
| mcp-docs-rag | 🔄 90% | Применить миграцию pgvector, проиндексировать |
| mcp-instagram | 🔄 50% | Базовая структура есть, нужна интеграция с FB Graph API |

---

### 🔴 ПРЕДСТОИТ СДЕЛАТЬ

#### Критический приоритет (масштабирование — см. Масштаб_и_стабильность.md)
| Модуль | Описание | Зависимости |
|--------|----------|-------------|
| **Разделение n8n** | 3 сервера: Transport + Processing + API | Серверы |
| **Python AI Worker** | Только AI extraction через Redis (~100 строк, не замена n8n) | Redis |
| **BAT IN Telegram** | Активировать workflow (MCP готов) | - |
| **BAT OUT VK/WhatsApp** | Активировать исходящие workflows | - |

#### Высокий приоритет
| Модуль | Описание | Зависимости |
|--------|----------|-------------|
| **Админ-панель** | Web UI для управления вертикалями | - |
| **10-20 платящих клиентов** | Proof для инвестора (План_Остановки.md) | Стабильная система |

#### Средний приоритет
| Модуль | Описание | Зависимости |
|--------|----------|-------------|
| Neo4j полноценный | Граф знаний для рекомендаций | - |
| CRM интеграции | AMO, Bitrix24 sync | Админка |
| Учёт интеграции | МойСклад, Remonline | Админка |
| pgvector + RAG | Семантический поиск (mcp-docs-rag) | Опционально |

#### Низкий приоритет (будущее)
| Модуль | Описание |
|--------|----------|
| AI Конструктор | Vertical SaaS Factory — диалог → вертикаль |
| Маркетплейс MCP | Публичные расширения |
| White Label | Кастомный брендинг |
| Питч Денису Сметневу | После 10-20 клиентов (контакт через Андрея) |

---

### 📊 Сводка

```
✅ Готово:        ~75% core функционала
🔄 В разработке:  2 модуля
🔴 Предстоит:     ~10 модулей разной сложности

Стратегия масштабирования: n8n ОСТАЁТСЯ, разделяем на 3 сервера + Python AI Worker
Главный блокер: Админ-панель + первые платящие клиенты

MCP серверы: 7 готово (telegram, vk, whatsapp, avito, max, form, api-android)
Multi-tenant: telegram, vk, whatsapp, avito, max (Redis + PostgreSQL storage)
```

---

## 📝 Принятые решения

> При принятии важного решения — добавь сюда. При изменении — не удаляй старое, добавь новое с датой.

### 2025-12-06: Multi-tenant MCP архитектура
**Решение:** Все MCP каналы v2.0.0 используют единый storage (Redis + PostgreSQL) с динамическими webhook URLs.
**Причина:** Масштабируемость — один инстанс MCP сервера обслуживает множество аккаунтов. Webhook URL: `/webhook/{channel}/{account_hash}` где hash = SHA256[:16] от primary credential.

### 2025-12-06: Два сервера для MCP
**Решение:** RU server (45.144.177.128) для avito/vk/max/form/android, Finnish server (217.145.79.27) для telegram/whatsapp.
**Причина:** Telegram и WhatsApp требуют стабильное соединение с внешними API, финский сервер ближе к EU датацентрам.

### 2025-12-06: RAG на PostgreSQL pgvector
**Решение:** Использовать pgvector вместо отдельного vector DB (Pinecone, Weaviate).
**Причина:** Всё в одной базе, проще инфраструктура, достаточная производительность для наших объёмов.

### 2025-12-05: MCP серверы отдельно от n8n
**Решение:** Каждый канал — отдельный MCP сервер на FastAPI.
**Причина:** Изоляция, масштабирование, переиспользование при миграции на Python.

### 2025-12-04: PostgreSQL как основная БД
**Решение:** PostgreSQL + JSONB вместо MongoDB.
**Причина:** Реляционные связи, транзакции, pgvector для embeddings.

---

## ❓ Открытые вопросы

> Когда вопрос решён — перенеси в "Принятые решения" с датой.

- [ ] **pgvector:** Установлен ли на production PostgreSQL? Проверить: `SELECT * FROM pg_extension WHERE extname = 'vector';`
- [ ] **Neo4j:** Разворачивать или обойтись рекурсивными CTE в PostgreSQL?
- [ ] **Админка:** Своя разработка или Retool/Appsmith?
- [ ] **Pricing MVP:** Какие цены для первых клиентов?

---

## 🔄 Процедуры обслуживания KB

### Ежедневно (при активной разработке)
```bash
# Синхронизация workflows из n8n
python scripts/full_sync.py --quick
```

### Еженедельно
```bash
# Полная синхронизация
python scripts/full_sync.py

# Обновление документации flows
python scripts/update_flow_docs.py --all

# Переиндексация RAG (если менялись Plans/)
python MCP/mcp-docs-rag/indexer.py
```

### При изменении архитектуры
1. Обнови компоненты в KB (add_component, add_relation)
2. Обнови этот файл (секции "Состояние", "Решения")
3. Обнови Plans/*.md если нужно
4. Переиндексируй RAG

---

## 🧭 Навигация по контексту

### Как понять "где мы"
1. Посмотри секцию "Состояние проекта" выше
2. Запроси активные компоненты: `SELECT type, name, category FROM project_components WHERE status IN ('active', 'in_progress') ORDER BY type;`
3. Посмотри последние изменения: `SELECT * FROM project_changelog ORDER BY changed_at DESC LIMIT 10;`

### Как понять "куда двигаемся"
1. Секция "Запланировано" выше
2. `Plans/Стратегия_Роста.md` — бизнес-цели
3. `Plans/Миграция_на_Python_Архитектура.md` — технический roadmap

### Как найти связи
```sql
-- Что зависит от компонента X
SELECT c.type, c.name, r.relation_type
FROM component_relations r
JOIN project_components c ON c.id = r.from_component_id
WHERE r.to_component_id = (SELECT id FROM project_components WHERE name = 'X');

-- От чего зависит компонент X
SELECT c.type, c.name, r.relation_type
FROM component_relations r
JOIN project_components c ON c.id = r.to_component_id
WHERE r.from_component_id = (SELECT id FROM project_components WHERE name = 'X');
```

---

## 📚 Ключевые документы

| Документ | Содержание | Когда читать |
|----------|------------|--------------|
| `Plans/Видение_AI_Конструктора.md` | Концепция продукта, 20+ блоков | Понимание "что строим" |
| `Plans/Миграция_на_Python_Архитектура.md` | Техническая архитектура, слои | Backend задачи |
| `Plans/Стратегия_Роста.md` | Бизнес-план, цены, таймлайн | Бизнес вопросы |
| `Plans/Админка_Спецификация.md` | UI админки, роли | Работа с админкой |
| `Plans/Тарифы_Ценообразование.md` | Тарифы Solo/Agency/Enterprise | Монетизация |
| `Plans/НАВИГАЦИЯ_ПРОЕКТА.md` | Mermaid графы связей | Визуализация |

---

## 🛠️ Быстрые команды

```bash
# === СИНХРОНИЗАЦИЯ ===
python scripts/full_sync.py              # Полная синхронизация KB
python scripts/full_sync.py --quick      # Быстрая (только workflows)

# === ПОИСК ===
python scripts/trace_flow.py "keyword"   # Трассировка по ключевому слову
python scripts/trace_flow.py --workflow "BAT IN Telegram"  # Трассировка workflow

# === ДОКУМЕНТАЦИЯ ===
python scripts/update_flow_docs.py --all # Обновить все flow docs
python scripts/update_flow_docs.py --index  # Показать индекс

# === RAG ===
python MCP/mcp-docs-rag/indexer.py       # Индексировать Plans/
python MCP/mcp-docs-rag/indexer.py --link      # Связать с KB
python MCP/mcp-docs-rag/indexer.py --components  # Embeddings для компонентов
```

---

## 📅 История изменений KB

### 2025-12-06 (вечер)
- Развёрнуты все MCP серверы v2.0.0 multi-tenant на production
- RU server: mcp-avito (8765), mcp-vk (8767), mcp-max (8768), mcp-form (8770), api-android (8780)
- Finnish server: mcp-telegram (8767), mcp-whatsapp (8766)
- Создан Redis (mcp-redis) на Finnish server для telegram/whatsapp
- Обновлён CONTEXT.md с актуальной информацией о серверах

### 2025-12-06 (утро)
- Создан MCP Docs RAG сервер
- Добавлена миграция pgvector (`20251206_docs_rag_embeddings.sql`)
- Создан этот файл CONTEXT.md
- Добавлены функции `search_docs()`, `search_components()`, `hybrid_search()`, `get_related_context()`

### 2025-12-05
- Создан shared storage для MCP серверов
- Добавлена таблица `channel_accounts`

### 2025-12-04
- Создана структура KB (project_components, component_relations)
- Созданы скрипты full_sync.py, trace_flow.py

---

*Последнее обновление структуры: 2025-12-06*
