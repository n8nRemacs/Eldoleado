# Eldoleado Admin Panel — Спецификация

> **Цель:** Мультивертикальная веб-админка с единым движком и настраиваемым UI под каждую вертикаль

---

## 1. Ключевой принцип: Мультивертикальность

### Концепция

```
┌─────────────────────────────────────────────────────────────────────┐
│                     ЕДИНЫЙ ДВИЖОК АДМИНКИ                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   Core Engine (общий код)                                          │
│   ├── Auth / Permissions                                           │
│   ├── CRUD операции                                                │
│   ├── API клиент                                                   │
│   └── UI компоненты (базовые)                                      │
│                                                                     │
│   Vertical Config (JSON/DB)                                        │
│   ├── Брендинг (лого, цвета, шрифты)                              │
│   ├── Терминология (клиент → пациент/гость/заказчик)              │
│   ├── Модули (вкл/выкл)                                           │
│   ├── Поля сущностей (какие показывать)                           │
│   └── Бизнес-логика (статусы, воронки)                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              │
           ┌──────────────────┼──────────────────┐
           ▼                  ▼                  ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   CRM Салона    │ │  CRM Автосервис │ │  CRM Клиника    │
│   красоты       │ │                 │ │                 │
├─────────────────┤ ├─────────────────┤ ├─────────────────┤
│ Клиенты         │ │ Клиенты         │ │ Пациенты        │
│ Записи          │ │ Заказ-наряды    │ │ Приёмы          │
│ Мастера         │ │ Механики        │ │ Врачи           │
│ Услуги          │ │ Работы+Запчасти │ │ Услуги          │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### Что настраивается под вертикаль

| Аспект | Салон красоты | Автосервис | Клиника |
|--------|---------------|------------|---------|
| **Клиент** | Клиент | Клиент/Авто | Пациент |
| **Обращение** | Запись | Заказ-наряд | Приём |
| **Исполнитель** | Мастер | Механик | Врач |
| **Услуга** | Услуга | Работа | Процедура |
| **Доп. сущности** | — | Автомобиль, Запчасти | Мед. карта |
| **Статусы** | Новая→Подтверждена→Завершена | Принят→В работе→Готов→Выдан | Записан→На приёме→Завершён |

### Структура VerticalConfig

```typescript
interface VerticalConfig {
  // Идентификация
  id: string;                    // "auto_service", "beauty_salon", "clinic"
  name: string;                  // "Автосервис", "Салон красоты", "Клиника"
  version: string;               // "1.0.0"

  // Брендинг
  branding: {
    logo?: string;               // URL логотипа
    primaryColor: string;        // "#FF5722"
    secondaryColor: string;      // "#FFC107"
    favicon?: string;
  };

  // Терминология — ключевой элемент мультивертикальности
  terminology: {
    client: { singular: string; plural: string; };        // "Клиент" / "Пациент"
    appeal: { singular: string; plural: string; };        // "Обращение" / "Заказ-наряд"
    executor: { singular: string; plural: string; };      // "Оператор" / "Механик"
    service: { singular: string; plural: string; };       // "Услуга" / "Работа"
    appointment: { singular: string; plural: string; };   // "Запись" / "Приём"
  };

  // Модули (вкл/выкл)
  modules: {
    calendar: boolean;           // Календарь записей
    inventory: boolean;          // Склад/запчасти
    vehicles: boolean;           // Автомобили (для автосервиса)
    medicalCards: boolean;       // Мед. карты (для клиник)
    loyalty: boolean;            // Программа лояльности
    invoicing: boolean;          // Счета и акты
    // ... расширяемо
  };

  // Сущность "Клиент" — какие поля показывать
  clientFields: {
    required: string[];          // ["name", "phone"]
    optional: string[];          // ["email", "birthdate", "notes"]
    custom: CustomField[];       // Кастомные поля вертикали
  };

  // Сущность "Обращение/Заказ"
  appealFields: {
    required: string[];
    optional: string[];
    custom: CustomField[];
  };

  // Статусы обращений (воронка)
  statuses: Status[];

  // AI настройки для вертикали
  ai: {
    defaultPrompt: string;       // Системный промпт для AI
    intentExamples: Intent[];    // Примеры намерений
    responseTemplates: Template[];
  };
}

interface CustomField {
  id: string;
  label: string;
  type: "text" | "number" | "date" | "select" | "multiselect" | "boolean";
  options?: string[];            // Для select/multiselect
  required: boolean;
  placeholder?: string;
}

interface Status {
  id: string;
  label: string;
  color: string;
  icon?: string;
  autoTransitions?: {            // Автопереходы
    after: number;               // Через N минут
    to: string;                  // В какой статус
    condition?: string;          // Условие (опционально)
  }[];
}

interface Intent {
  name: string;                  // "booking", "price_inquiry", "complaint"
  examples: string[];            // Примеры фраз
  action: string;                // Что делать при обнаружении
}
```

### Пример конфига: Автосервис

```json
{
  "id": "auto_service",
  "name": "Автосервис",
  "version": "1.0.0",

  "branding": {
    "primaryColor": "#1565C0",
    "secondaryColor": "#FFC107"
  },

  "terminology": {
    "client": { "singular": "Клиент", "plural": "Клиенты" },
    "appeal": { "singular": "Заказ-наряд", "plural": "Заказ-наряды" },
    "executor": { "singular": "Механик", "plural": "Механики" },
    "service": { "singular": "Работа", "plural": "Работы" },
    "appointment": { "singular": "Запись на ремонт", "plural": "Записи на ремонт" }
  },

  "modules": {
    "calendar": true,
    "inventory": true,
    "vehicles": true,
    "medicalCards": false,
    "loyalty": true,
    "invoicing": true
  },

  "clientFields": {
    "required": ["name", "phone"],
    "optional": ["email"],
    "custom": []
  },

  "appealFields": {
    "required": ["vehicle_id", "description"],
    "optional": ["desired_date", "urgency"],
    "custom": [
      {
        "id": "mileage",
        "label": "Пробег",
        "type": "number",
        "required": false,
        "placeholder": "км"
      },
      {
        "id": "work_type",
        "label": "Тип работ",
        "type": "multiselect",
        "options": ["ТО", "Ремонт ходовой", "Двигатель", "Электрика", "Кузовные", "Шиномонтаж"],
        "required": true
      }
    ]
  },

  "statuses": [
    { "id": "new", "label": "Новый", "color": "#2196F3", "icon": "inbox" },
    { "id": "confirmed", "label": "Подтверждён", "color": "#4CAF50", "icon": "check" },
    { "id": "waiting_parts", "label": "Ожидание запчастей", "color": "#FF9800", "icon": "package" },
    { "id": "in_progress", "label": "В работе", "color": "#9C27B0", "icon": "wrench" },
    { "id": "ready", "label": "Готов", "color": "#4CAF50", "icon": "check-circle" },
    { "id": "issued", "label": "Выдан", "color": "#607D8B", "icon": "car" }
  ],

  "ai": {
    "defaultPrompt": "Ты — виртуальный помощник автосервиса. Помогаешь клиентам записаться на ремонт, узнать статус заказа, ответить на вопросы о ценах и услугах.",
    "intentExamples": [
      {
        "name": "booking",
        "examples": ["хочу записаться", "нужен ремонт", "когда можно приехать"],
        "action": "create_booking"
      },
      {
        "name": "status_check",
        "examples": ["как там моя машина", "готов ли заказ", "статус ремонта"],
        "action": "check_order_status"
      }
    ]
  }
}
```

### Пример конфига: Клиника

```json
{
  "id": "medical_clinic",
  "name": "Медицинская клиника",
  "version": "1.0.0",

  "terminology": {
    "client": { "singular": "Пациент", "plural": "Пациенты" },
    "appeal": { "singular": "Приём", "plural": "Приёмы" },
    "executor": { "singular": "Врач", "plural": "Врачи" },
    "service": { "singular": "Процедура", "plural": "Процедуры" },
    "appointment": { "singular": "Запись к врачу", "plural": "Записи к врачам" }
  },

  "modules": {
    "calendar": true,
    "inventory": false,
    "vehicles": false,
    "medicalCards": true,
    "loyalty": false,
    "invoicing": true
  },

  "clientFields": {
    "required": ["name", "phone", "birthdate"],
    "optional": ["email", "address"],
    "custom": [
      {
        "id": "insurance_policy",
        "label": "Полис ОМС/ДМС",
        "type": "text",
        "required": false
      },
      {
        "id": "allergies",
        "label": "Аллергии",
        "type": "text",
        "required": false,
        "placeholder": "Известные аллергии"
      }
    ]
  },

  "statuses": [
    { "id": "scheduled", "label": "Записан", "color": "#2196F3" },
    { "id": "confirmed", "label": "Подтверждён", "color": "#4CAF50" },
    { "id": "waiting", "label": "В очереди", "color": "#FF9800" },
    { "id": "in_progress", "label": "На приёме", "color": "#9C27B0" },
    { "id": "completed", "label": "Завершён", "color": "#607D8B" }
  ],

  "ai": {
    "defaultPrompt": "Ты — виртуальный администратор медицинской клиники. Помогаешь пациентам записаться к врачу, узнать расписание, ответить на вопросы об услугах. ВАЖНО: Никогда не давай медицинских рекомендаций.",
    "intentExamples": [
      {
        "name": "booking",
        "examples": ["записаться к терапевту", "нужен врач", "когда приём"],
        "action": "create_appointment"
      }
    ]
  }
}
```

### Архитектура баз данных: Config DB + Business DB

> **Принцип:** Настройки ядра и вертикалей — отдельно от бизнес-данных тенантов.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    РАЗДЕЛЕНИЕ БАЗ ДАННЫХ                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                     CONFIG DB (Core)                              │ │
│  │                  Настройки ядра и вертикалей                      │ │
│  │                                                                   │ │
│  │  • vertical_configs      — Шаблоны вертикалей                    │ │
│  │  • field_definitions     — Определения полей                     │ │
│  │  • status_definitions    — Определения статусов                  │ │
│  │  • ai_configs            — AI промпты и правила                  │ │
│  │  • module_configs        — Настройки модулей                     │ │
│  │  • ui_themes             — Темы оформления                       │ │
│  │  • feature_flags         — Флаги функций                         │ │
│  │  • marketplace_items     — Каталог маркетплейса                  │ │
│  │                                                                   │ │
│  │  Характеристики:                                                 │ │
│  │  • Редко меняется (обновления платформы)                         │ │
│  │  • Кэшируется в Redis                                            │ │
│  │  • Одна на всю платформу (shared)                                │ │
│  │  • Бэкап: раз в день достаточно                                  │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                              │                                          │
│                              │ vertical_id                              │
│                              ▼                                          │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                    BUSINESS DB (Tenants)                          │ │
│  │                 Данные тенантов и клиентов                        │ │
│  │                                                                   │ │
│  │  • tenants               — Список тенантов                       │ │
│  │  • tenant_settings       — Переопределения конфигов              │ │
│  │  • operators             — Операторы тенантов                    │ │
│  │  • channels              — Подключённые каналы                   │ │
│  │  • clients               — Клиенты/пациенты                      │ │
│  │  • appeals               — Обращения/заказы                      │ │
│  │  • messages              — Сообщения                             │ │
│  │  • sessions              — Сессии чатов                          │ │
│  │  • custom_field_values   — Значения кастомных полей              │ │
│  │  • ...                   — Остальные бизнес-данные               │ │
│  │                                                                   │ │
│  │  Характеристики:                                                 │ │
│  │  • Часто меняется (каждое сообщение)                             │ │
│  │  • Изолировано по tenant_id                                      │ │
│  │  • Бэкап: постоянный (streaming replication)                     │ │
│  │  • Шардинг при росте                                             │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Схема Config DB

```sql
-- =====================================================
-- CONFIG DB: Настройки ядра платформы
-- =====================================================

-- 1. Вертикали (шаблоны бизнес-ниш)
CREATE TABLE vertical_configs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    vertical_id VARCHAR(50) UNIQUE NOT NULL,  -- "auto_service", "clinic"
    name VARCHAR(100) NOT NULL,               -- "Автосервис"
    description TEXT,
    version VARCHAR(20) DEFAULT '1.0.0',

    -- Основные настройки (JSONB для гибкости)
    branding JSONB DEFAULT '{}',              -- logo, colors, fonts
    terminology JSONB NOT NULL,               -- client, appeal, executor...
    modules JSONB DEFAULT '{}',               -- calendar, inventory, etc.

    is_system BOOLEAN DEFAULT false,          -- Системные нельзя удалить
    is_active BOOLEAN DEFAULT true,
    sort_order INT DEFAULT 0,

    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 2. Определения полей для сущностей
CREATE TABLE field_definitions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    vertical_id VARCHAR(50) REFERENCES vertical_configs(vertical_id),
    entity_type VARCHAR(50) NOT NULL,         -- 'client', 'appeal', 'service'
    field_id VARCHAR(50) NOT NULL,            -- 'mileage', 'insurance_policy'

    label VARCHAR(100) NOT NULL,
    field_type VARCHAR(20) NOT NULL,          -- 'text', 'number', 'select'
    options JSONB,                            -- Для select/multiselect

    is_required BOOLEAN DEFAULT false,
    is_system BOOLEAN DEFAULT false,          -- Системные (name, phone)
    is_searchable BOOLEAN DEFAULT false,      -- Индексировать для поиска
    sort_order INT DEFAULT 0,

    placeholder VARCHAR(200),
    default_value TEXT,
    validation JSONB,                         -- { min, max, pattern, etc. }

    UNIQUE(vertical_id, entity_type, field_id)
);

-- 3. Определения статусов (воронки)
CREATE TABLE status_definitions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    vertical_id VARCHAR(50) REFERENCES vertical_configs(vertical_id),

    status_id VARCHAR(50) NOT NULL,
    label VARCHAR(100) NOT NULL,
    color VARCHAR(20) NOT NULL,               -- "#4CAF50"
    icon VARCHAR(50),                         -- "check-circle"

    sort_order INT DEFAULT 0,
    is_initial BOOLEAN DEFAULT false,         -- Начальный статус
    is_final BOOLEAN DEFAULT false,           -- Конечный статус

    auto_transitions JSONB,                   -- Автопереходы по времени
    allowed_transitions TEXT[],               -- В какие статусы можно перейти

    UNIQUE(vertical_id, status_id)
);

-- 4. AI конфигурации
CREATE TABLE ai_configs (
    vertical_id VARCHAR(50) PRIMARY KEY REFERENCES vertical_configs(vertical_id),

    system_prompt TEXT NOT NULL,              -- Базовый промпт
    intent_examples JSONB DEFAULT '[]',       -- Примеры намерений
    extraction_rules JSONB DEFAULT '{}',      -- Правила извлечения данных
    response_templates JSONB DEFAULT '[]',    -- Шаблоны ответов

    model_preferences JSONB DEFAULT '{}',     -- Предпочтения по моделям

    updated_at TIMESTAMP DEFAULT NOW()
);

-- 5. Настройки модулей
CREATE TABLE module_configs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    vertical_id VARCHAR(50) REFERENCES vertical_configs(vertical_id),
    module_id VARCHAR(50) NOT NULL,           -- 'calendar', 'inventory'

    is_enabled BOOLEAN DEFAULT true,
    settings JSONB DEFAULT '{}',              -- Настройки модуля

    UNIQUE(vertical_id, module_id)
);

-- 6. Темы оформления
CREATE TABLE ui_themes (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    theme_id VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,

    colors JSONB NOT NULL,                    -- primary, secondary, etc.
    fonts JSONB,                              -- font families
    spacing JSONB,                            -- margins, paddings
    components JSONB,                         -- button styles, etc.

    is_default BOOLEAN DEFAULT false
);

-- 7. Флаги функций (feature flags)
CREATE TABLE feature_flags (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    flag_id VARCHAR(100) UNIQUE NOT NULL,
    description TEXT,

    is_enabled BOOLEAN DEFAULT false,
    enabled_for JSONB DEFAULT '[]',           -- Конкретные tenant_ids
    rollout_percentage INT DEFAULT 0,         -- % тенантов (0-100)

    created_at TIMESTAMP DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_field_defs_vertical ON field_definitions(vertical_id);
CREATE INDEX idx_field_defs_entity ON field_definitions(vertical_id, entity_type);
CREATE INDEX idx_status_defs_vertical ON status_definitions(vertical_id);
CREATE INDEX idx_module_configs_vertical ON module_configs(vertical_id);
```

### Схема Business DB (таблицы настроек тенантов)

```sql
-- =====================================================
-- BUSINESS DB: Данные тенантов
-- =====================================================

-- Тенанты (основная таблица)
CREATE TABLE tenants (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(200) NOT NULL,
    slug VARCHAR(100) UNIQUE,                 -- URL-friendly имя

    -- Связь с Config DB
    vertical_id VARCHAR(50) NOT NULL,         -- → config_db.vertical_configs
    theme_id VARCHAR(50),                     -- → config_db.ui_themes

    -- Статус
    status VARCHAR(20) DEFAULT 'active',      -- active, suspended, deleted
    plan VARCHAR(50) DEFAULT 'solo',          -- solo, agency, enterprise

    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Переопределения конфигов тенанта
CREATE TABLE tenant_settings (
    tenant_id UUID PRIMARY KEY REFERENCES tenants(id),

    -- Переопределения (мержатся с vertical_config)
    branding_override JSONB DEFAULT '{}',     -- Свой логотип, цвета
    terminology_override JSONB DEFAULT '{}',  -- Своя терминология
    modules_override JSONB DEFAULT '{}',      -- Вкл/выкл модули

    -- AI переопределения
    ai_prompt_override TEXT,                  -- Дополнение к промпту
    ai_settings JSONB DEFAULT '{}',           -- Настройки AI

    -- Кастомные поля тенанта (сверх вертикали)
    custom_fields JSONB DEFAULT '[]',
    custom_statuses JSONB DEFAULT '[]',

    updated_at TIMESTAMP DEFAULT NOW()
);

-- Значения кастомных полей
CREATE TABLE custom_field_values (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES tenants(id),
    entity_type VARCHAR(50) NOT NULL,         -- 'client', 'appeal'
    entity_id UUID NOT NULL,                  -- ID клиента/обращения
    field_id VARCHAR(50) NOT NULL,            -- ID поля из field_definitions

    value_text TEXT,                          -- Для text
    value_number NUMERIC,                     -- Для number
    value_date TIMESTAMP,                     -- Для date
    value_json JSONB,                         -- Для select/multiselect/complex

    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),

    UNIQUE(tenant_id, entity_type, entity_id, field_id)
);

-- Индексы
CREATE INDEX idx_tenants_vertical ON tenants(vertical_id);
CREATE INDEX idx_tenant_settings_tenant ON tenant_settings(tenant_id);
CREATE INDEX idx_custom_fields_entity ON custom_field_values(tenant_id, entity_type, entity_id);
CREATE INDEX idx_custom_fields_field ON custom_field_values(tenant_id, field_id);
```

### Как это работает вместе

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        ЗАПРОС КОНФИГА ТЕНАНТА                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. Запрос от админки: GET /admin/config                               │
│                              │                                          │
│                              ▼                                          │
│  2. Проверка Redis кэша: tenant:{id}:config                            │
│        │                                                                │
│        ├── HIT → Вернуть из кэша                                       │
│        │                                                                │
│        └── MISS ↓                                                       │
│                                                                         │
│  3. Загрузка из Business DB:                                           │
│     ┌──────────────────────────────────────────┐                       │
│     │ SELECT vertical_id FROM tenants          │                       │
│     │ WHERE id = $tenant_id                    │                       │
│     │                                          │                       │
│     │ SELECT * FROM tenant_settings            │                       │
│     │ WHERE tenant_id = $tenant_id             │                       │
│     └──────────────────────────────────────────┘                       │
│                              │                                          │
│                              ▼                                          │
│  4. Загрузка из Config DB:                                             │
│     ┌──────────────────────────────────────────┐                       │
│     │ SELECT * FROM vertical_configs           │                       │
│     │ WHERE vertical_id = $vertical_id         │                       │
│     │                                          │                       │
│     │ SELECT * FROM field_definitions          │                       │
│     │ WHERE vertical_id = $vertical_id         │                       │
│     │                                          │                       │
│     │ SELECT * FROM status_definitions         │                       │
│     │ WHERE vertical_id = $vertical_id         │                       │
│     │                                          │                       │
│     │ SELECT * FROM ai_configs                 │                       │
│     │ WHERE vertical_id = $vertical_id         │                       │
│     └──────────────────────────────────────────┘                       │
│                              │                                          │
│                              ▼                                          │
│  5. Мерж конфигов:                                                     │
│     ┌──────────────────────────────────────────┐                       │
│     │ baseConfig = vertical_configs            │                       │
│     │ + field_definitions                      │                       │
│     │ + status_definitions                     │                       │
│     │ + ai_configs                             │                       │
│     │                                          │                       │
│     │ tenantConfig = deepMerge(                │                       │
│     │   baseConfig,                            │                       │
│     │   tenant_settings.*_override             │                       │
│     │ )                                        │                       │
│     └──────────────────────────────────────────┘                       │
│                              │                                          │
│                              ▼                                          │
│  6. Сохранить в Redis + вернуть                                        │
│     redis.set(`tenant:${id}:config`, JSON.stringify(config))           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Инвалидация кэша

```typescript
// При изменении вертикали (Config DB)
async function onVerticalConfigChanged(verticalId: string) {
  // Найти всех тенантов этой вертикали
  const tenants = await businessDb.query(
    'SELECT id FROM tenants WHERE vertical_id = $1',
    [verticalId]
  );

  // Инвалидировать кэш каждого
  const keys = tenants.map(t => `tenant:${t.id}:config`);
  await redis.del(...keys);
}

// При изменении настроек тенанта (Business DB)
async function onTenantSettingsChanged(tenantId: string) {
  await redis.del(`tenant:${tenantId}:config`);
}
```

### Преимущества разделения

| Аспект | Config DB | Business DB |
|--------|-----------|-------------|
| **Частота изменений** | Редко (релизы) | Постоянно (сообщения) |
| **Бэкап** | Раз в день | Streaming replication |
| **Размер** | Маленький (МБ) | Большой (ГБ-ТБ) |
| **Кэширование** | Агрессивное (TTL часы) | По запросу (TTL минуты) |
| **Шардинг** | Не нужен | По tenant_id |
| **Self-hosted** | Может быть read-only копия | Полная копия |
| **Доступ** | Только чтение для тенантов | Read/Write |

### Как админка использует конфиг

```typescript
// hooks/useVerticalConfig.ts
export function useVerticalConfig() {
  const { tenant } = useAuth();

  const { data: config } = useQuery({
    queryKey: ['vertical-config', tenant.vertical_id],
    queryFn: () => api.get(`/admin/config/vertical/${tenant.vertical_id}`)
  });

  // Мержим базовый конфиг с переопределениями тенанта
  const mergedConfig = useMemo(() => {
    if (!config) return null;
    return deepMerge(config, tenant.config_override || {});
  }, [config, tenant.config_override]);

  return mergedConfig;
}

// Использование в компонентах
function ClientsPage() {
  const config = useVerticalConfig();

  return (
    <div>
      <h1>{config.terminology.client.plural}</h1>
      {/* Отображает "Клиенты" или "Пациенты" в зависимости от вертикали */}

      <DataTable
        columns={buildColumns(config.clientFields)}
        // Колонки динамически строятся из конфига
      />
    </div>
  );
}

function AppealsPage() {
  const config = useVerticalConfig();

  return (
    <div>
      <h1>{config.terminology.appeal.plural}</h1>
      {/* "Обращения" или "Заказ-наряды" или "Приёмы" */}

      <StatusFilter statuses={config.statuses} />
      {/* Статусы из конфига вертикали */}
    </div>
  );
}
```

---

## 2. Почему отдельная веб-админка (не в Android)

### Проблемы встраивания в Android

| Проблема | Влияние |
|----------|---------|
| Размер APK | +5-10 MB на админ-интерфейс |
| Безопасность | Админские API в клиентском коде |
| Обновления | Каждое изменение = релиз в Play Store |
| Целевая аудитория | Операторы ≠ Владельцы бизнеса |
| UX | Админка с телефона — неудобно |

### Преимущества веб-админки

- **Мгновенные обновления** — деплой и готово
- **Работает везде** — ноутбук, планшет, телефон (в крайнем случае)
- **Безопасность** — отдельный домен, отдельная авторизация
- **Не зависит от магазинов** — никаких ревью

---

## 3. Разделение ролей: Android vs Web

```
┌─────────────────────────────────────────────────────────────────────┐
│                        ANDROID APP                                   │
│                      (Оператор/Менеджер)                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  • Список обращений (inbox)                                        │
│  • Чат с клиентом                                                  │
│  • Перевод обращения другому оператору                             │
│  • Закрытие обращения                                              │
│  • Базовая статистика (мои обращения за день)                      │
│  • Push-уведомления о новых сообщениях                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                        WEB ADMIN                                     │
│                   (Владелец/Администратор)                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  • Управление тенантом (настройки компании)                        │
│  • Управление операторами (добавить/удалить/права)                 │
│  • Настройка каналов (Telegram, WhatsApp, VK, Avito)               │
│  • Воронки и автоматизация                                         │
│  • AI настройки (промпты, поведение)                               │
│  • Биллинг и подписки                                              │
│  • Аналитика и отчёты                                              │
│  • Экспорт данных                                                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4. Структура админки по тарифам

### 4.1. Индивидуальный тариф (Solo)

**URL:** `app.eldoleado.ru`

```
Главная
├── Дашборд (сводка за день/неделю/месяц)
├── Обращения (веб-версия inbox)
├── Клиенты (база клиентов)
├── Каналы
│   ├── Подключённые каналы
│   └── Маркетплейс каналов
├── Автоматизация
│   ├── Автоответы
│   ├── Триггеры
│   └── AI настройки
├── Аналитика
│   ├── Обращения
│   ├── Операторы (только я)
│   └── Каналы
└── Настройки
    ├── Профиль
    ├── Подписка
    └── Интеграции
```

### 4.2. Агентский тариф (Agency)

**URL:** `agent.eldoleado.ru`

```
Панель агента
├── Мои клиенты (список тенантов)
│   ├── [Клиент 1] → переключение
│   ├── [Клиент 2] → переключение
│   └── + Добавить клиента
├── Сводная аналитика
│   ├── По всем клиентам
│   ├── Сравнение клиентов
│   └── Топ/Проблемные
├── Шаблоны вертикалей
│   ├── Мои шаблоны
│   └── Создать шаблон
├── Массовые действия
│   ├── Обновить настройки у всех
│   └── Рассылка клиентам
└── Биллинг агента
    ├── Моя подписка
    └── Статистика использования

--- При переключении на клиента ---

[Клиент: Салон красоты "Мария"]
├── Дашборд клиента
├── Обращения
├── Каналы
├── Автоматизация
├── Аналитика
└── Настройки клиента
```

### 4.3. Энтерпрайз тариф (Enterprise)

**URL:** `admin.{company-domain}.ru` (self-hosted)

```
Корпоративная панель
├── Дашборд компании
├── Структура
│   ├── Филиалы
│   ├── Отделы
│   └── Группы
├── Сотрудники
│   ├── Все сотрудники
│   ├── Роли и права
│   └── Активность
├── Обращения
│   ├── Все
│   ├── По филиалам
│   └── По отделам
├── Каналы
│   ├── Корпоративные каналы
│   └── По филиалам
├── Автоматизация
│   ├── Корпоративные правила
│   └── Правила филиалов
├── Аналитика
│   ├── Сводная
│   ├── По филиалам
│   ├── По сотрудникам
│   └── SLA
├── Интеграции
│   ├── Active Directory
│   ├── 1С / SAP
│   └── API
└── Администрирование
    ├── Настройки системы
    ├── Бэкапы
    ├── Логи
    └── Обновления
```

### 4.4. Коммерческая вертикаль (Commercial Vertical)

**URL:** `admin.{vertical-brand}.ru`

```
Панель владельца вертикали
├── Мои клиенты (тенанты)
│   ├── Список клиентов
│   ├── + Добавить клиента
│   └── Импорт клиентов
├── Тарифные планы
│   ├── Мои тарифы (Лайт/Стандарт/Премиум)
│   ├── Что включено в каждый
│   └── Управление лимитами
├── Брендинг
│   ├── Логотип
│   ├── Цвета
│   ├── Домен
│   └── Email шаблоны
├── Шаблон вертикали
│   ├── Базовые настройки
│   ├── Каналы по умолчанию
│   ├── Автоматизация по умолчанию
│   └── AI промпты для ниши
├── Аналитика
│   ├── По всем клиентам
│   ├── Использование ресурсов
│   └── Доходы
├── Биллинг
│   ├── Мои платежи Eldoleado
│   ├── Платежи клиентов мне
│   └── Настройка эквайринга
└── Маркетплейс
    ├── Мои расширения
    └── Опубликовать расширение
```

---

## 5. Ключевые экраны

### 5.1. Дашборд

```
┌─────────────────────────────────────────────────────────────────────┐
│  ELDOLEADO                                    [Салон "Мария" ▼]     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Сегодня                              Неделя                        │
│  ┌──────────────┐ ┌──────────────┐   ┌────────────────────────────┐│
│  │ 47           │ │ 12           │   │ ████████████               ││
│  │ обращений    │ │ новых        │   │ █████████                  ││
│  │ ↑ 12% к вчера│ │ клиентов     │   │ ██████████████             ││
│  └──────────────┘ └──────────────┘   │ Обращения по дням          ││
│                                      └────────────────────────────┘│
│  ┌──────────────┐ ┌──────────────┐                                 │
│  │ 3.2 мин      │ │ 94%          │   Каналы                        │
│  │ среднее      │ │ закрыто      │   ┌────────────────────────────┐│
│  │ время ответа │ │ сегодня      │   │ Telegram  ████████  65%    ││
│  └──────────────┘ └──────────────┘   │ WhatsApp  ████      28%    ││
│                                      │ VK        █          7%    ││
│  Требуют внимания                    └────────────────────────────┘│
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ ⚠ 3 обращения без ответа > 30 мин                              ││
│  │ ⚠ Канал WhatsApp: ошибка авторизации                          ││
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2. Управление каналами

```
┌─────────────────────────────────────────────────────────────────────┐
│  Каналы                                              [+ Добавить]   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Подключённые                                                       │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ 🟢 Telegram    @salon_maria_bot     Активен    [Настройки]     ││
│  │ 🟢 WhatsApp    +7 (999) 123-45-67   Активен    [Настройки]     ││
│  │ 🔴 VK          vk.com/salon_maria   Ошибка     [Переподключить]││
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
│  Доступные каналы (Маркетплейс)                                    │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ Telegram Bot   Бесплатно (свой бот)              [Подключить]  ││
│  │ WhatsApp API   от 2000 ₽/мес                     [Подключить]  ││
│  │ VK API         Бесплатно (свой токен)            [Подключить]  ││
│  │ Avito          от 1500 ₽/мес                     [Подключить]  ││
│  │ Instagram      от 3000 ₽/мес                     [Скоро]       ││
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3. Управление операторами

```
┌─────────────────────────────────────────────────────────────────────┐
│  Операторы                                           [+ Добавить]   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ Фото  Имя              Роль         Статус    Сегодня          ││
│  ├────────────────────────────────────────────────────────────────┤│
│  │ 👤    Анна Петрова     Оператор     🟢 Online  23 обращения    ││
│  │ 👤    Иван Сидоров     Оператор     🟡 Away    15 обращений    ││
│  │ 👤    Мария Козлова    Админ        🔴 Offline 0 обращений     ││
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
│  Добавить оператора                                                │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ Email: [                    ]                                  ││
│  │ Роль:  [Оператор ▼]                                           ││
│  │ Каналы: ☑ Telegram  ☑ WhatsApp  ☐ VK                          ││
│  │                                           [Отправить приглашение]│
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.4. AI Настройки

```
┌─────────────────────────────────────────────────────────────────────┐
│  AI Настройки                                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Режим работы AI                                                   │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ ○ Выключен                                                     ││
│  │ ● Помощник (подсказки оператору)                               ││
│  │ ○ Автопилот (отвечает сам, сложные → оператору)               ││
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
│  Описание бизнеса (для контекста AI)                               │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ Салон красоты "Мария". Услуги: маникюр, педикюр, стрижки,     ││
│  │ окрашивание. Работаем с 10:00 до 20:00, без выходных.         ││
│  │ Адрес: ул. Ленина, 15. Запись через Telegram или по телефону. ││
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
│  Стиль общения                                                     │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ ○ Формальный (Вы, полные предложения)                         ││
│  │ ● Дружелюбный (ты/Вы, эмодзи допустимы)                       ││
│  │ ○ Неформальный (ты, разговорный стиль)                        ││
│  └────────────────────────────────────────────────────────────────┘│
│                                                                     │
│  Автоматические действия                                           │
│  ☑ Извлекать контакты из сообщений                                │
│  ☑ Определять намерение клиента                                   │
│  ☑ Предлагать время записи                                        │
│  ☐ Автоматически создавать задачи                                 │
│                                                                     │
│                                                    [Сохранить]      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6. Технологии

### 6.1. Рекомендуемый стек

| Компонент | Технология | Почему |
|-----------|------------|--------|
| **Frontend** | React + TypeScript | Стандарт, большое сообщество |
| **UI Kit** | Tailwind + shadcn/ui | Быстрая разработка, кастомизация |
| **State** | Zustand или React Query | Простота, без бойлерплейта |
| **Routing** | React Router | Стандарт |
| **API** | REST (n8n webhooks) | Уже есть инфраструктура |
| **Auth** | JWT + Refresh tokens | Стандарт |
| **Hosting** | Vercel / Cloudflare Pages | Бесплатно, CDN |

### 6.2. Альтернативы (быстрый старт)

| Вариант | Время | Гибкость | Комментарий |
|---------|-------|----------|-------------|
| **Retool** | 1-2 дня | Низкая | No-code, быстрый прототип |
| **Appsmith** | 1-2 дня | Средняя | Open-source, self-hosted |
| **Refine** | 3-5 дней | Высокая | React framework для админок |
| **Next.js + shadcn** | 1-2 недели | Максимальная | Полный контроль |

### 6.3. Архитектура

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Web Admin     │────►│   n8n API       │────►│   PostgreSQL    │
│   (React SPA)   │     │   (webhooks)    │     │   (Supabase)    │
└─────────────────┘     └─────────────────┘     └─────────────────┘
        │                       │
        │                       │
        ▼                       ▼
┌─────────────────┐     ┌─────────────────┐
│   Vercel CDN    │     │   Redis         │
│   (hosting)     │     │   (sessions)    │
└─────────────────┘     └─────────────────┘
```

---

## 7. API для админки

### 7.1. Endpoints (n8n webhooks)

```
POST /admin/auth/login         - Авторизация
POST /admin/auth/logout        - Выход
POST /admin/auth/refresh       - Обновление токена

GET  /admin/dashboard          - Данные дашборда
GET  /admin/dashboard/stats    - Статистика

GET  /admin/operators          - Список операторов
POST /admin/operators          - Добавить оператора
PUT  /admin/operators/:id      - Обновить оператора
DELETE /admin/operators/:id    - Удалить оператора

GET  /admin/channels           - Список каналов
POST /admin/channels           - Подключить канал
PUT  /admin/channels/:id       - Обновить канал
DELETE /admin/channels/:id     - Отключить канал

GET  /admin/automation/rules   - Правила автоматизации
POST /admin/automation/rules   - Создать правило
PUT  /admin/automation/rules/:id
DELETE /admin/automation/rules/:id

GET  /admin/ai/settings        - Настройки AI
PUT  /admin/ai/settings        - Обновить настройки AI

GET  /admin/analytics          - Аналитика
GET  /admin/analytics/export   - Экспорт данных

GET  /admin/settings           - Настройки тенанта
PUT  /admin/settings           - Обновить настройки

GET  /admin/billing            - Биллинг
POST /admin/billing/subscribe  - Подписаться на тариф
```

### 7.2. Авторизация

```
Headers:
  Authorization: Bearer <jwt_token>
  X-Tenant-ID: <tenant_uuid>

JWT Payload:
{
  "sub": "user_uuid",
  "tenant_id": "tenant_uuid",
  "role": "admin|operator",
  "permissions": ["manage_operators", "manage_channels", ...],
  "exp": 1234567890
}
```

---

## 8. Безопасность

### 8.1. Разделение доступа

| Роль | Права |
|------|-------|
| **Owner** | Полный доступ, биллинг, удаление тенанта |
| **Admin** | Всё кроме биллинга и удаления |
| **Manager** | Операторы, каналы, аналитика |
| **Operator** | Только просмотр своей статистики |

### 8.2. Меры безопасности

- HTTPS only
- JWT с коротким TTL (15 мин) + Refresh token (7 дней)
- Rate limiting на API
- CORS только с разрешённых доменов
- CSP headers
- Audit log всех действий

---

## 9. Домены

| Тариф | Домен |
|-------|-------|
| Solo / Agency | `app.eldoleado.ru` |
| Agent Dashboard | `agent.eldoleado.ru` |
| Enterprise | `admin.{company}.ru` (self-hosted) |
| Commercial Vertical | `admin.{brand}.ru` (white-label) |

---

## 10. Этапы разработки

### MVP (2-3 недели)

- [ ] Авторизация (login/logout)
- [ ] Дашборд (базовые метрики)
- [ ] Управление операторами (CRUD)
- [ ] Управление каналами (CRUD)
- [ ] Базовые настройки тенанта

### v1.0 (4-6 недель)

- [ ] AI настройки
- [ ] Правила автоматизации
- [ ] Аналитика (графики, экспорт)
- [ ] Биллинг интеграция
- [ ] Уведомления

### v2.0 (8-12 недель)

- [ ] Агентский дашборд (мультитенант)
- [ ] White-label настройки
- [ ] Расширенная аналитика
- [ ] API для интеграций
- [ ] Мобильная версия (responsive)

---

## 11. Открытые вопросы

- [ ] Выбор UI фреймворка (Retool для MVP или сразу React?)
- [ ] Хостинг (Vercel бесплатно или свой сервер?)
- [ ] Интеграция биллинга (ЮKassa, Tinkoff?)
- [ ] Нужен ли offline режим для админки?
- [ ] Real-time обновления (WebSocket или polling?)

---

*Документ создан: декабрь 2025*
*Версия: 1.0*
*Автор: Claude (Opus 4.5) + Human*
